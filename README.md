# BetterTiermaker
Better tiermaker is a TierMaker clone, but without ads.

The program allows the user to import their own images, optionally labeled, and rank them on a tier list. 

The project is made using the react library
